<%= dropdown do %>
  <%= dropdown_button do %>
    <%# DeleteThis - insert link / button / icon / etc. %>
  <% end %>
  <%# DeleteThis - dropdown item can be either a simple link or custom code in a block %>
  <%= dropdown_items do %>
    <% dropdown_item(
      text: 'ChangeDisplay',
      link: ChangePathOrUrl,
      # DeleteThis - if you want to show that a certain item is the current selection, add bg-primary text-white
      class: "#{'bg-primary text-white' if ChangeLogic}"
    ) %>
    <% dropdown_item class: "#{'bg-primary text-white' if ChangeLogic}" do %>
    <% end %>
  <% end %>
<% end %>
